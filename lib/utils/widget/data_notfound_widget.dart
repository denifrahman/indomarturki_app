import 'package:flutter/material.dart';

class DataNotFound extends StatelessWidget {
  DataNotFound({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text('Data tidak ditemukan'),
    );
  }
}
